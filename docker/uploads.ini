; Cloud Run refuses a request body over 32 MiB. Matching the limits here means
; WordPress rejects an oversized upload with its own message rather than the
; platform returning a bare 413 the admin cannot explain.
upload_max_filesize=32M
post_max_size=32M
max_execution_time=300
memory_limit=512M
