# 8080, hardcoded, matching container_port in Terraform.
#
# Apache's ${PORT} expansion resolves Define directives and the envvars file,
# not the process environment Cloud Run injects. Writing `Listen ${PORT}` here
# produces a container that starts, logs nothing useful, and never binds — so
# the port is a constant on both sides instead.
Listen 8080
